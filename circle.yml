machine:
  ruby:
    version: 2.3.0

dependencies:
  override:
    - bundle install
    - jekyll build

deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync ./_site s3://octavius-blog/blog/ --delete
